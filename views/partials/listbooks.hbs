{{#if singleBooklist.books}}

  <table class="ui unstackable striped table">
    <thead>
      <tr>
        <th>Title</th>
        <th>Author</th>
      </tr>
    </thead>
    <tbody>
      {{#each singleBooklist.books}}
        <tr>
          <td>
            {{title}}
          </td>
          <td>
            {{author}}
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>

  {{else}}
    <h3 class="ui message">
      Empty booklist!
    </h3>

{{/if}}
